name: Staging Edge Gate

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
  workflow_dispatch:
    inputs:
      run_service_role_tests:
        description: "Run service_role smoke tests (A3/B4/C4)"
        required: false
        default: false
        type: boolean
      git_ref:
        description: "Optional git ref/sha to validate"
        required: false
        default: ""
        type: string

concurrency:
  group: staging-edge-gate
  cancel-in-progress: false

jobs:
  smoke-only:
    # `head_branch == 'main'` NÃO identifica este repositório: num PR vindo de
    # um fork cujo branch se chama `main`, a execução do CI dispara este
    # workflow_run com head_branch == 'main' e head_sha do COMMIT DO FORK.
    # Como o passo de checkout usa esse sha e o job roda um script do próprio
    # commit, o segredo do job iria para código de terceiro. Só push no
    # repositório de origem passa.
    if: >
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'push' &&
       github.event.workflow_run.head_branch == 'main' &&
       github.event.workflow_run.head_repository.full_name == github.repository) ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      RUN_SERVICE_ROLE_TESTS: ${{ github.event_name == 'workflow_dispatch' && inputs.run_service_role_tests || 'false' }}

    steps:
      - name: Checkout (CI-triggered SHA)
        if: github.event_name == 'workflow_run'
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Checkout (manual ref)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.git_ref != '' && inputs.git_ref || github.sha }}

      # A chave de serviço só entra no ambiente dos passos que a usam, e só
      # quando os testes de service_role foram pedidos à mão.
      - name: Validate required secrets
        env:
          SUPABASE_SERVICE_ROLE_KEY: ${{ (github.event_name == 'workflow_dispatch' && inputs.run_service_role_tests) && secrets.SUPABASE_SERVICE_ROLE_KEY || '' }}
        run: |
          missing=0
          for v in SUPABASE_URL SUPABASE_ANON_KEY; do
            if [ -z "${!v}" ]; then
              echo "::error::Missing required secret: ${v}"
              missing=1
            fi
          done
          if [ "${RUN_SERVICE_ROLE_TESTS}" = "true" ] && [ -z "${SUPABASE_SERVICE_ROLE_KEY}" ]; then
            echo "::error::Missing required secret: SUPABASE_SERVICE_ROLE_KEY"
            missing=1
          fi
          exit "${missing}"

      - name: Run edge auth smoke tests
        env:
          SUPABASE_SERVICE_ROLE_KEY: ${{ (github.event_name == 'workflow_dispatch' && inputs.run_service_role_tests) && secrets.SUPABASE_SERVICE_ROLE_KEY || '' }}
        run: bash ./scripts/ci-smoke-edge-auth.sh

      - name: Upload smoke report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: edge-smoke-report
          path: artifacts/edge-smoke-report.md
          retention-days: 14
