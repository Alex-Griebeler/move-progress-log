/**
 * Rodapé fixo no fundo de um DialogContent que rola (p-6): a ação principal
 * fica sempre visível no celular, sem precisar rolar até o fim do formulário
 * (revisão UX-09). Compensa o padding do diálogo para encostar nas bordas.
 */
export const STICKY_FOOTER_CLASS =
  "sticky bottom-0 z-20 -mx-6 -mb-6 gap-2 border-t bg-background/95 px-6 py-3 backdrop-blur";
